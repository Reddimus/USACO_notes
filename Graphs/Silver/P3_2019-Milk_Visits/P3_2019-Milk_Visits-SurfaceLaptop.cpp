#include <bits/stdc++.h>

using namespace std;

int main() {
	freopen("milkvisits.in", "r", stdin);
    int n, m;
    cin >> n >> m;
    char preferences[n];
    for (int i = 0; i < n; i++)
        cin >> preferences[i];
    
}